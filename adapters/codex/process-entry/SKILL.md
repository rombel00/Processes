---
name: process-entry
description: Начать или продолжить содержательную проектную задачу по Processes — восстановить крупный блок roadmap и его фазу, выполнить разрешённый пакет и передать результат. Не применять для обычных вопросов или автоматического подключения всех проектов.
---

# Вход в Processes

Сообщи об использовании навыка. Сначала прочитай проектные инструкции и
проверь, есть ли `.process/installation.json` в рабочем проекте.

До проектных изменений покажи проверенный паспорт старта: блок/часть/подблок/DoD,
**фактическую модель, рекомендуемую модель и краткую причину**, полный рабочий
путь, репозиторий, ветку/HEAD,
незакоммиченные изменения и окружение/данные. Неизвестное, отсутствие Git
или detached HEAD укажи явно; не раскрывай секреты. Если cwd задачи отличается
от рабочего пути/worktree — назови оба. Несовпадение с передачей сначала
проясни, не переключай ветку и не переноси файлы молча. Повтори при смене
места/ветки/окружения; включи эти ориентиры в передачу, не в каждый ответ.

- Если manifest есть: используй установленный `.agents/skills/process-workflow/SKILL.md`
  и пути из manifest. Не подменяй версию процесса исходником и не переустанавливай
  его при каждом новом чате. При конфликте правил предложи обновление отдельно.
- Если manifest нет: прочитай `source.md` рядом с этим навыком (локальный указатель,
  создаваемый при установке), затем WORKING_AGREEMENT и
  `adapters/codex/process-workflow/SKILL.md` из указанного исходника.
  Если указатель/исходник недоступен — сообщи, не выдумывай пути.
  Прочитать правила можно без установки; запись проектных файлов — в рамках
  согласованного подключения/пакета, не по одному наличию этого навыка.

Восстанови крупный блок roadmap (например, MVP) и часть: тонкий scope блока,
фундамент архитектуры/безопасности, итерационная delivery с текущим подблоком
либо приёмка/релиз. Владелец может просто
описать желаемое: сам сообщи, где мы, что делает этот чат и когда понадобится
новый. Scope и фундамент по умолчанию — отдельные пользовательские задачи.
Delivery может вести несколько связанных подблоков подряд в одном чате: для
каждого подробный C → impact check → дельта только при необходимости → plan →
код, тесты/review → интеграция. Уточни цель/DoD, предъяви план и границы; после
работы дай доказательства, ссылку на передачу и STOP. Перед ним прочитай roadmap по пути из manifest
(до подключения — по согласованному сопоставлению/реестру PROCESS) и
связанный предметный plan/пакет: выдай компактное «Что дальше» только по этим
источникам и готовый промт следующей задачи с путями, целью/DoD, моделью,
проверками и стопом. Если продолжение не сохранено — не выдумывай его; промт
должен вести на планирование или приёмку родительского DoD.
Новое пожелание классифицируй как уточнение, изменение блока, возврат на прошлую
фазу либо backlog; не расширяй scope молча. Не принимай согласование одной фазы
за разрешение следующей. На внутреннем шаге не создавай новый чат; обычный
ответ не требует документов.
