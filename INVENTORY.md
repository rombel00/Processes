# Инвентаризация: что берём, что переписываем, чего не хватает

**Статус:** Шаг 2. Всё, что есть под рукой, разложено по фазам из
[`PROCESS.md`](plugins/process-core/PROCESS.md). По каждой единице — решение и причина.

**Чего здесь нет:** самих текстов скиллов. Это Шаги 5–6.

---

## Источники

**Что откуда взято и на каких условиях — в [`SOURCES.md`](SOURCES.md).**
Здесь только решения по единицам; дублировать атрибуцию нельзя, иначе следующий
источник допишут в тот файл, куда посмотрят первым, а сверять их будет нечем.

Один факт, относящийся к решениям: четыре импортированных скилла **побайтово
совпадают** с апстримом — расхождений за время использования не накопилось,
можно переписывать без сверки.

---

## Решения по фазам

Категории: **адаптировать** — привести к контракту, правки косметические ·
**распилить** — содержание годное, но файл втрое больше лимита ·
**переписать** — взять идею, текст свой · **новый** — нет ни у кого.

### `frame` → `brief`

| Единица | Откуда | Решение |
|---|---|---|
| `brief-writing` | есть (yng) | **адаптировать** — 146 строк, Job Story + RICE, качественный. Добавить контракт, формат эскалации |
| `brief-reviewer` | — | **новый** субагент |

### `discovery` → `market`, `personas`, `interview_report`

| Единица | Откуда | Решение |
|---|---|---|
| `market-research` | есть (yng) | **адаптировать** — 141 строка, Graph-of-Thought, метки достоверности 🟢🟡🔴 |
| `persona-generation` | yng, не взят | **взять + адаптировать** — 127 строк. Даёт anti-persona и early adopter, чего нет больше нигде |
| `persona-interview` | yng, не взят | **взять + адаптировать** — 129 строк. Режим «адвокат дьявола» — самая дешёвая проверка идеи до живых людей |

Ранее эти два скилла были отложены как «рассчитанные на внешних пользователей».
Пересматриваю: обе работают на **гипотезах**, а не на данных от живых людей, и
именно поэтому применимы до того, как пользователи появятся. Ограничение
«это симуляция, а не голос рынка» в них уже прописано автором.

### `strategy` → `lean_canvas`

| Единица | Откуда | Решение |
|---|---|---|
| `lean-canvas` | есть (yng) | **адаптировать** — 110 строк, 9 блоков по Маурье |
| `cognitive-biases` | файл | **адаптировать в `process-core`** — линза, а не фаза. Вызов остаётся явным |

### `definition` → `story_map`, `roadmap`

| Единица | Откуда | Решение |
|---|---|---|
| `user-story-mapping` | есть (yng) | **адаптировать** — 115 строк, backbone + нарезка по релизам |
| `release-planning` | — | **новый**, маленький. Продюсер `roadmap`: последовательность релизов за пределами первого среза |
| `story-map-reviewer` | — | **новый** субагент |

### `design` → `wireframes`, `ia`

| Единица | Откуда | Решение |
|---|---|---|
| `wireframe-spec` | yng, не взят | **взять + адаптировать** — 109 строк, Unicode-вайрфреймы. Плотный, но в лимит проходит |
| `information-architecture` | yng, не взят | **взять + переписать** — 49 строк, контракта в frontmatter нет вообще |
| `user-journey-map` | yng, не взят | **взять + переписать**, но **без своего артефакта** — 47 строк, диагностический. Пишет раздел внутрь `ia` по триггеру, тем же правилом, что и реестр ADR |

### `handoff` → `task_spec`

| Единица | Откуда | Решение |
|---|---|---|
| `handoff-spec` | — | **новый**. Ключевой скилл системы |
| `handoff-reviewer` | — | **новый** субагент |

Пусто у всех. Ровно тот стык, ради которого затевалась пересборка.
Материал для добора нефункциональных требований можно взять из структуры
аналитика `rdudov` (`02`, раздел 3), но роль другая — не автор, а переводчик.

### `architecture` → `architecture`

| Единица | Откуда | Решение |
|---|---|---|
| `architecture-design` | rdudov `04`, 653 строки | **распилить** — SKILL.md ~150 + `references/` |
| `architecture-reviewer` | rdudov `05`, 474 строки | **распилить** в субагента + `references/` |
| `architecture-repair` | rdudov `04a`, 46 строк | **адаптировать** — полная триада только здесь и в коде |
| ~~`adr`~~ | — | **отложен**. Решения ведутся разделом внутри `architecture`; отдельный реестр заводится по триггеру первой сквозной ссылки ([`PROCESS.md`](plugins/process-core/PROCESS.md)) |

### `planning` → `plan`

| Единица | Откуда | Решение |
|---|---|---|
| `task-planning` | rdudov `06`, 315 строк | **распилить** |
| ~~`plan-reviewer`~~ | rdudov `07`, 256 строк | **не брать как ревьюера** — гейта на `planning` нет ([`PROCESS.md`](plugins/process-core/PROCESS.md)). Критерии покрытия переезжают чеклистом внутрь планировщика |
| ~~`planning-repair`~~ | rdudov `06a` | **не брать** — без гейта не нужен |

### `implementation` → код, `test_report`

| Единица | Откуда | Решение |
|---|---|---|
| `implementation` | rdudov `08`, 547 строк | **распилить**. Оттуда же правило «сначала заглушки, потом наполнение» и запрет считать ветку проверенной по mock-тестам |
| `code-reviewer` | rdudov `09`, 597 строк | **распилить** в субагента |
| `implementation-repair` | rdudov `08a`, 49 строк | **адаптировать** |

### `launch` → `release_notes`

| Единица | Откуда | Решение |
|---|---|---|
| `launch-check` | — | **новый**. Проверка по пункту 9 `task_spec`, готовность отката |

### `learn` → `learnings`

| Единица | Откуда | Решение |
|---|---|---|
| `retro` | — | **новый**. Замыкает петлю в `frame` |

### Процессные (вне фаз)

| Единица | Откуда | Решение |
|---|---|---|
| `orchestrate` | rdudov `01`, 729 строк + `10`, 80 строк | **переписать**. Идеи берём (status.md, лимит циклов, минимальный контекст, checkpoint), текст свой: у нас другие фазы, облегчённая пара и нет внешнего CLI. Сюда же инлайном вливается разбор блокеров |
| ~~`blocker-triage`~~ | rdudov `10` | **не заводить отдельным скиллом** — таксономия уже описана в `process-core/PROCESS.md`, второй файл пришлось бы синхронизировать. И это не методология по нашему же определению: нет шагов и структуры вывода, есть проверка по двум спискам |
| `independent-review` | своё, `cto-review` | ✅ **сделано.** Паттерн вынесен в `templates/REVIEWER_TEMPLATE.md`, скилл переписан начисто: generic-ревью вне гейтов, пути берутся из реестра, вердикт в JSON по контракту |

---

## Что не берём

| Единица | Почему |
|---|---|
| `presentation-code-interpreter` (yng, 402 строки + 4 файла) | Генератор PPTX, а не шаг процесса. В Claude Code есть нативный `pptx` |
| `analyst` + `tz-reviewer` + `analysis-repair` (rdudov `02`, `03`, `02a`, 630 строк) | Их работа поглощена продуктовым треком и фазой `handoff` — решение Шага 1. Писать ТЗ поверх story map значило бы завести второй источник правды |
| `plan-reviewer`, `planning-repair` (rdudov `07`, `06a`) | Гейта на `planning` нет; критерии переезжают чеклистом |
| `_agent_development.txt` (rdudov) | Дубль `00_agent_development.md` |

Отказ от аналитика — самое крупное сокращение: **630 строк исходника не
переносятся**, потому что решение о стыке принято на Шаге 1.

---

## Правки к карте Шага 1

Инвентаризация вскрыла три расхождения между реестром и тем, что реально
кто-то производит. Как и ожидалось — это её работа.

1. **`scope` удалён как отдельный артефакт.** Его никто не производит:
   нарезка по релизам с явным MVP уже внутри `story_map`. Гейт
   `scope-review` → `story-map-review`.
2. **`ux_spec` разложен на `wireframes` + `ia`.** Под него нашлось несколько
   разных скиллов, а правило гласит «один скилл — один артефакт». Заодно
   совпало с наименованием у `yngchefcook`. Третий кандидат, `journey_map`,
   своего идентификатора не получил — см. ниже.
3. **`interviews` → `interview_report`** — имя из импортируемого скилла.

**`roadmap` остался без продюсера** — ни в одном источнике нет скилла, который
его строит. Пробел закрывается новым скиллом `release-planning` в волне 1:
нарезка на релизы за пределами первого среза оказалась востребована сразу.

Правки внесены в [`PROCESS.md`](plugins/process-core/PROCESS.md).

---

## Структурное изменение: `product-design` отдельным плагином

Под фазу `design` набралось три самостоятельных скилла. В `product-definition`
они не помещаются по смыслу: `product-definition` — обязательный хребет, а
`design` в карте помечена как опциональная (продукту без интерфейса вайрфреймы
не нужны, а доменный пак `game-design` этот слой вообще замещает).

**Предлагаю `product-design` отдельным плагином.** Альтернатива — сложить их в
`product-definition` и потерять возможность не ставить.

Обратимо на Шаге 3 одной командой, если не согласен.

**`legal` пока пуст** — контента под него нет ни в одном источнике. Плагин
создаётся в волне 2, когда появится хоть один скилл.

---

## Доменный пак `game-design` (волна 2)

Первый доменный пак — замещающий, а не аддитивный
([`ARCHITECTURE.md`](ARCHITECTURE.md) §1). Каркас не трогает: фазы, гейты,
контракты и весь delivery-трек остаются общими.

Что переносится в игры **как есть:** `brief`, `market` (жанр, похожие
адаптации, что не взлетело), `personas` (игроки), `lean_canvas`, весь
delivery-трек, `launch`, `learn`.

Что **замещается:**

| Фаза | Универсальный метод | Почему не работает | Замена |
|---|---|---|---|
| `definition` | `user-story-mapping` | Игра — не линейный путь пользователя. «Как игрок я хочу…» разваливается на втором шаге | core loop, мета-луп, прогрессия |
| `design` | `wireframe-spec` | Годится для меню, магазина и HUD, но не для игрового поля и не для визуального языка | арт-дирекшен + UI-слой |

Что **добавляется** — специфика адаптации настольной игры: что
автоматизировать, что упростить, чем заменить физические аффордансы
(тактильность компонентов, чтение соперника, разговор за столом).

Открытые вопросы к проектированию пака — на волну 2:

- Живая проверка (пункт 9 `task_spec`) в играх — это плейтест. Нужен ли
  отдельный скилл или достаточно записи в `task_spec`?
- Ассеты и графика тянут за собой лицензионный вопрос. Возможно, это первый
  реальный контент для пустого плагина `legal`.

---

## Итоговый состав

| Плагин | Скиллы | Субагенты | Обязателен |
|---|---|---|---|
| `process-core` | orchestrate, independent-review, cognitive-biases | — | да |
| `product-definition` | brief-writing, lean-canvas, user-story-mapping, release-planning, handoff-spec, retro | brief-reviewer, story-map-reviewer, handoff-reviewer | да |
<!-- Оба обязательны, то есть сегодня всегда ставятся вместе, и разделение
пока не покупает гибкости установки. Обоснование — что process-core сможет
обслуживать delivery без продуктового трека — гипотеза волны 2. Проверить
после Шага 6: если delivery всё равно окажется зависим от product-definition,
слить в один плагин, а не защищать разделение задним числом. -->

| `product-discovery` | market-research, persona-generation, persona-interview | — | нет |
| `product-design` | wireframe-spec, information-architecture, user-journey-map | — | нет |
| `delivery` | architecture-design, architecture-repair, task-planning, implementation, implementation-repair, launch-check | architecture-reviewer, code-reviewer | нет |
| `game-design` | — (волна 2, доменный пак) | — | нет |
| `legal` | — (волна 2) | — | нет |

**Каталог плагина заводится вместе с первым своим скиллом, не заранее.**
На Шаге 3 это три плагина — `process-core`, `product-definition`,
`product-discovery`: только у них к этому моменту есть содержимое.
`product-design` и `delivery` появляются в волне 2 вместе со своими скиллами,
`game-design` и `legal` — позже.

(Ранее здесь стояло «пять». Пересчитано по факту: у `product-design` и
`delivery` на Шаге 3 нет ни одного скилла, а пустой каталог с манифестом,
который ничего не устанавливает, — то же заведение сущности впрок, от
которого отказались с ADR и `journey_map`.)

Отдельного плагина `critique` нет: в нём был один скилл, а `premortem` и
`red-team` — пока намерение, а не контент. Линза переезжает в `process-core`;
если критических инструментов станет три и больше, выделим обратно. Заодно
исчезает третье значение «рекомендуется» в колонке обязательности — оно ни на
что не влияло, плагин либо стоит, либо нет.

**26 единиц:**

| Происхождение | Сколько | Что именно |
|---|---|---|
| Адаптируем имеющееся | 5 | brief-writing, lean-canvas, user-story-mapping, market-research, independent-review |
| Берём у `yngchefcook` | 5 | persona-generation, persona-interview, wireframe-spec, information-architecture, user-journey-map |
| Распиливаем и переписываем у `rdudov` | 8 | architecture-design, architecture-reviewer, architecture-repair, task-planning, implementation, code-reviewer, implementation-repair, orchestrate |
| Пишем с нуля | 7 | brief-reviewer, release-planning, story-map-reviewer, handoff-spec, handoff-reviewer, launch-check, retro |
| Линза | 1 | cognitive-biases |

Пустых фаз не осталось. Из семи написанных с нуля единиц четыре приходятся на
`handoff`, `launch` и `learn`; пятой фактически новой оказывается
`orchestrate` — от источника там остаются идеи, но не текст. То есть на то,
чего нет ни в одном внешнем источнике, приходится большая часть новой работы —
и это ровно то, что делает набор скиллов системой.

---

## Правки после ревью `vibe-critic`

Проект Шагов 0–2 прошёл через критическое ревью на предмет раздутости
(`.claude/agents/vibe-critic.md`, оценка избыточности 3/5). Принято пять
находок; раздутость оказалась сосредоточена в delivery-треке — он был
унаследован из командного пайплайна `rdudov` без пересчёта на одного человека.

| Находка | Что изменилось |
|---|---|
| `code-review` на каждой задаче плана | Гейт сдвинут на задачу доставки целиком. Оценка полного прохода функции: было 18–20 прогонов агентов, стало 10–12 |
| `adr` отдельным артефактом | Отложен до триггера первой сквозной ссылки |
| Сводная таблица моделей | Убрана: источник правды — frontmatter субагента. Вместо неё политика по классам ролей |
| `blocker-triage` отдельным скиллом | Влит в `orchestrate` |
| «Гейт пропустить нельзя» | Смягчено: механизма принуждения нет и не планируется, ответственность на владельце |

Отдельно зафиксировано как ограничение к Шагу 4: ручной, никем не
принуждаемый запуск — свойство, а не недоделка ([`ARCHITECTURE.md`](ARCHITECTURE.md)).

---

## Приоритет волн 5–6

Владелец — продакт, discovery он ведёт компетенцией, а не инструкцией скилла.
Ресерч-трек не наращивается сверх уже решённого в этой инвентаризации:
`market-research`, `persona-generation`, `persona-interview` — и всё, никаких
дополнительных discovery-скиллов сверх плана.

Вес идёт туда, где ценность системы для владельца выше: **`product-definition`**
(проработка сценариев — story map, стык, нарезка релизов) и **`delivery`**
(архитектура, разработка, тестирование). При конфликте приоритетов в волнах
5–6 выигрывает эта пара, не discovery.

---

## Что дальше

**Шаг 3** ✅ — скелет репозитория: три плагина, переезд файлов, манифесты,
атрибуция вынесена в [`SOURCES.md`](SOURCES.md). Содержимое скиллов не тронуто.

**Шаг 4** — оркестратор и состояние.
</content>
