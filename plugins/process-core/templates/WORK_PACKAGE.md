# Пакет <package-id>: <результат>

> Редакция: <ID> · Этап roadmap: <ссылка>
> Снимок логики на согласование; текущий статус и допуск — в status/approvals.

- Крупный блок roadmap / текущая часть / подблок: <ID блока, ID части, task-id при наличии и ссылки>.
- Тип работы: scope блока / фундамент архитектуры/безопасности / итерационная delivery / приёмка/релиз.
- Work type / execution mode: <один из канонических route и mode>.
- Expected evidence / decision gate / investment cap: <ссылка, порог и лимит; либо причина неприменимости>.
- Входящая передача: <status и источники; при продолжении — прежний допуск>.
- Место исполнения: <рабочая папка/worktree, репозиторий, ветка/база и окружение;
  фактическое состояние на старте/при передаче — в status, не менять снимок пакета>.

## План и логика для согласования

- Что изменится для пользователя / потребителя результата:
- Сценарии, переходы/макеты, бизнес-правила (для продуктовых изменений — ссылки):
- Границы, затрагиваемые файлы/данные и исключения:
- Зафиксированные входы и их версии (для delivery — принятый фундамент и scope):
- Разрешённые производные результаты пакета (например C, impact, адресная
  дельта, plan, код/отчёты) и где фиксируются их фактические версии/гейты:
- Последовательность внутренних шагов (для delivery: список подблоков и для
  каждого C → impact → delta при необходимости → plan → реализация/проверки →
  интеграция; новый пользовательский чат на каждый шаг не требуется):
- Неизвестное, которое блокирует зависимую работу:

## Проверка и исполнение

- Definition of Done, доказательства и приёмка:
- Режим проверки: <baseline/recovery | high-risk | routine | low-risk; причины>.
- Риски → достаточные доказательства; сгруппированные проверки / применимые
  гейты / пределы повторов:
- Условие повторного полного regression; лимит независимых вызовов (для
  development `code-review` — ровно 1 на разрешённый implementation-проход
  подблока, повтор только как отдельное
  явно обоснованное решение владельца):
- Влияние на каноническую документацию: <что обновляется или почему достаточно
  кода, тестов и отчёта>.
- Фактическая модель; рекомендованная модель / рассуждение и причины:
- Текущая или новая задача и причины:
- Агенты / параллельная работа, если явно разрешены:
- Разрешённые Git и внешние действия / расходы:
- Данные и окружение: <тестовые/синтетические; что запрещено; отдельные допуски>.
- Точная точка остановки и исключения:
- External actions, PII, деньги и prod: <явно запрещены либо точный отдельный допуск>.
- Запись согласия на эту редакцию в approvals:

## Результат и продолжение

При подключении Codex P2 после согласования этот файл неизменяем: допуск
связан с его точными байтами. До согласования указать здесь, в каком отдельном
отчёте/status будет результат. Следующие поля вести в том отчёте, не дописывая
их в утверждённый пакет. Новая логика — новая редакция и запись согласия.

- Сделано и доказательства:
- Ограничения и открытые вопросы:
- Проверенная редакция / commit:
- Место передачи: <фактическая папка/worktree, репозиторий, ветка/HEAD,
  незакоммиченные изменения и окружение; ожидаемое место продолжения, если другое>:
- Приёмка владельца (ссылка, не заполнять за него):
- Следующий разрешённый шаг или «стоп, ждём решения»:
- Источник плана продолжения: <roadmap путь/редакция; связанный plan/раздел или «нет»>.
- Что дальше: <только сохранённые узлы/зависимости; текущая или новая задача; модель; нужный допуск>.
- Готовый промт следующей задачи: <пути, узел/родитель, результат, цель/DoD,
  проверки, границы и STOP; если плана нет — промт на планирование/приёмку>.
- Stage-completion card: <результат; DoD/доказательства; ограничения; одна
  рекомендация и причина; next_chat: continue/new_recommended/new_required с
  причиной; фактическая/рекомендуемая модель; reviewer/лимит; разрешения;
  источники; следующий prompt>.
