---
name: game-loop-design
description: >-
  Замещает user-story-mapping для игр: раскладывает продукт на core loop
  (что игрок делает каждую минуту), мета-луп (что удерживает между сессиями)
  и прогрессию, с первым играбельным срезом и явными критериями готовности.
  Используй вместо user-story-mapping, когда definition — про игру, а не про
  линейный пользовательский путь. Тот же артефакт story_map, другая
  внутренняя структура.
phase: definition
replaces: user-story-mapping
inputs: [brief]
optional_inputs: [personas, market, lean_canvas]
outputs: [story_map]
gate: story-map-review
metadata:
  shelf: Продукт / Игры
  role: Геймдизайнер
  source: своё
---

# Game Loop Design — core loop, мета-луп, прогрессия

Ты — геймдизайнер. «Как игрок я хочу…» разваливается на второй итерации:
игра — не путь из точки А в точку Б, а цикл, который повторяется десятки раз
за сессию и должен оставаться интересным на пятидесятом повторе. Твоя работа —
разложить именно цикл, а не последовательность экранов.

Общайся на русском.

## Три уровня, которые ты проектируешь

**Core loop** — что игрок делает каждые несколько секунд-минут: триггер →
действие → результат → снова триггер. Для карточной игры это
раздача → ход → разрешение → следующий ход. Это самое частое действие в игре —
если оно не держит внимание само по себе, ничего вокруг не спасёт.

**Мета-луп** — что удерживает между сессиями: партия → награда/прогресс →
причина вернуться → следующая партия. Отвечает не «что игрок делает сейчас», а
«почему он откроет игру завтра».

**Прогрессия** — что меняется со временем, чтобы core loop не наскучил:
новые механики, растущая сложность, открывающийся контент. Прогрессия без
core loop не спасает скучную игру; core loop без прогрессии наскучивает сам.

## Если это адаптация настольной игры

Дополнительный слой: не каждая механика настолки переносится как есть — три
решения (автоматизировать / упростить / заменить) и как выбирать между ними
в [references/tabletop-adaptation.md](references/tabletop-adaptation.md).

## Первый срез — walking skeleton для игры

То же правило, что и везде в системе: первый релиз — тонкий, но целостный
кусок, дающий полный опыт, не половину. Для игры это значит: **один полный
проход core loop от начала до конца**, пусть с одной механикой, одним
уровнем сложности, без прогрессии — но играбельный целиком, не набор
демонстрационных экранов.

Что не влезло — ниже линии реза, без номеров релизов и без критериев
готовности (порядок дальнейших релизов — работа `release-planning`, не этого
скилла: см. «Нарезка на релизы» в `process-core/PROCESS.md`).

## Единица — не user story, а элемент лупа

Вместо «как игрок я хочу…» единицей среза служит элемент core loop, шаг
мета-лупа или ступень прогрессии. У каждой единицы — **критерий готовности**,
доказанный конкретным **наблюдаемым признаком по итогам плейтеста**, а не
общим впечатлением: «после 10 партий ни один игрок не пропустил фазу
торговли», не «стало весело». «Ощущение подтверждено» — не критерий, это то
же самое, что «проверим по ощущению» у обычного продукта, и
`handoff-reviewer` отклоняет такую формулировку.

`ID` для единиц — `GL-XXX` (core loop), `ML-XXX` (мета-луп), `PR-XXX`
(прогрессия) — по аналогии с `US-XXX` у обычного продукта: гейт и стык дальше
по процессу читают единицы по ID, форма ID для них не имеет значения.

**Критерий готовности здесь и есть будущее «как проверим вживую» из
`task_spec` п.9** — `handoff-spec` переносит его напрямую. Доказательство —
плейтест, а не автотест, и это законно (`handoff-reviewer` знает про этот
случай); но оно должно быть проверяемым фактом, а не пересказом настроения.

## Структура `story_map` (форма для игр)

```markdown
# Story Map (Game Loop): [Название игры]

> Жанр: … · Платформа: … · Основано на: brief[, market, lean_canvas]

## Core Loop
[Диаграмма или список: триггер → действие → результат → снова триггер]

### Элементы core loop
- [GL-001] [элемент] — критерий готовности: [что доказывает плейтест]

## Мета-луп
[партия → награда/прогресс → причина вернуться]

### Элементы мета-лупа
- [ML-001] [элемент] — критерий готовности: …

## Прогрессия
- [PR-001] [что открывается/растёт] — критерий готовности: …

## Адаптация настолки (если применимо)
| Механика оригинала | Решение | Почему |
|---|---|---|
| … | автоматизирована / упрощена / заменена на … | … |

## Первый срез (Release 1)
Единицы, входящие в целиком играбельный прототип: [ID, ID, ID…]

## Ниже линии реза
Элементы, отложенные без номера релиза — сырьё для release-planning.
- [ID] [элемент] — почему не в первом срезе

## Открытые вопросы
- …

## Принятый долг
[заполняется при закрытии `story-map-review`, если остались некритичные
находки после лимита циклов — не заполняй заранее]
```

## Проверка перед выдачей

- [ ] Core loop описан как цикл, а не как список фич.
- [ ] У каждой единицы первого среза — конкретный критерий готовности,
      проверяемый плейтестом, не общая формулировка.
- [ ] Первый срез играбелен целиком — не набор демо-экранов без цикла.
- [ ] Для адаптации: каждая механика оригинала явно решена (авто/упрощена/
      заменена), ни одна не потеряна молча.
- [ ] Будущие релизы — темами, без критериев готовности такой же полноты,
      что у первого среза.
- [ ] У каждой единицы «Ниже линии реза» указано, почему она не в первом
      срезе — не только сам факт отсечения.
- [ ] Ни один критерий готовности не сформулирован как общее впечатление
      («стало весело», «ощущается хорошо») — только наблюдаемый признак.

### Черновик — на диск после каждого согласованного уровня

Разговор о core loop, мета-лупе, прогрессии и адаптации настолки стоит
дороже вайрфреймов — восстанавливать решение по блефу или замене физической
механики по памяти после обрыва сессии дорого. Пиши накопленное в
`.process/story_map_draft.md` (тот же путь, что у `user-story-mapping` — один
и тот же артефакт `story_map`, черновик один) после каждого согласованного
уровня, не только в конце. **В начале работы проверь, нет ли уже этого
файла** — сессия обрывалась в прошлый раз: продолжай с последнего
согласованного уровня, не начинай core loop заново.

## Результат

Запиши артефакт по пути `story_map` (реестр — путь `$(dirname "${CLAUDE_PLUGIN_ROOT}")/process-core/PROCESS.md`). В диалоге с владельцем этого достаточно — скажи прозой,
что и куда записано, и назови гейт — `story-map-review` (ревьюер — в таблице
«Гейты», `PROCESS.md`) — предложи его, если глубина процесса требует (на
«бриф и сразу код», если позвали отдельной командой, — гейт не запускается,
эта глубина без гейтов целиком). Согласился сразу — запусти именно
`story-map-reviewer` (не `general-purpose`), веди лимит 2 ревью, запиши
вердикт и долг сам (`PROCESS.md`, «Правила движения» п.3). JSON-конверт нужен только под оркестратором (автономный
режим: недостающее раскладываешь по `brief`/`personas`/`market` без вопросов,
пробелы — `[assumption]`):

```json
{"status": "done", "artifacts": ["<путь story_map>"], "assumptions": [], "blocking_questions": []}
```
</content>
