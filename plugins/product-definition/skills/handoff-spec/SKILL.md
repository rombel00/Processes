---
name: handoff-spec
description: >-
  Переводит срез story_map (или его аналог у доменного пака) в task_spec —
  документ, с которого стартует delivery. Не пишет ТЗ заново: берёт готовое,
  добирает то, что продуктовый трек по своей природе не даёт — нефункциональные
  требования, точки интеграции, ограничения, — и фиксирует, как поймём, что
  сделанное работает. Используй, когда story_map готов и пора передавать срез
  в разработку.
phase: handoff
inputs: [story_map, brief]
optional_inputs: [lean_canvas, wireframes, ia]
outputs: [task_spec]
gate: handoff-review
metadata:
  shelf: Процесс
  role: Технический переводчик
  source: своё
---

# Handoff Spec — перевод и добор, не постановка заново

Ты — переводчик, не автор. `story_map` уже содержит юзер-кейсы с критериями
приёмки (или их аналог — core loop с условиями готовности, если домен другой).
Переписывать это своими словами значит завести второй источник правды рядом с
первым. Твоя работа — **сослаться**, добрать недостающее и проверить
достаточность.

Общайся на русском.

## Единица среза — форма не фиксирована

`story_map` для обычного продукта содержит user stories. Для доменного пака
(например, игры) — core loop и его шаги. Это один и тот же артефакт с разной
внутренней структурой ([`ARCHITECTURE.md`](../../../ARCHITECTURE.md) §1 —
доменный пак меняет метод, не идентификатор). Читай `story_map` как есть, не
предполагай заранее, что внутри «истории»: единицей может быть история, шаг
core loop или пункт `brief`, если `definition` продюсировала не карту, а что-то
своей формы.

Во всём, что ниже, «единица среза» — то, что реально нашлось в `story_map`.

## Разрез (`task-id`)

Один `task_spec` — один разрез: обычно Релиз 1 из `story_map` при первом
прогоне, следующий непереданный релиз — при повторных. Если неочевидно, какой
именно — спроси владельца, не гадай.

`task-id` — короткий kebab-slug по теме разреза: `oplata-podpiski`,
`import-csv`. Каталог: `docs/delivery/<task-id>/`.

## Шаги

1. **Прочитай `story_map` и `brief`.** Возьми ровно тот срез, который передаёшь.
   Не читай будущие релизы — они не твой объём.

2. **Что делаем (п.1).** Ссылка на срез — какие единицы входят, одной строкой
   на единицу. Не пересказ содержимого, а список идентификаторов
   (`US-003`, `US-007`…) со ссылкой на файл.

3. **Критерии приёмки (п.2).** Скопируй явно, по одному на единицу. Это
   единственное намеренное дублирование — критерии приёмки предмет спора при
   сдаче, delivery не должен ходить за ними в другой документ.

4. **Не делаем (п.3).** Out of scope явным списком. Бери из `story_map`
   (что ниже линии реза) и из `brief` (Out of Scope раздел), не изобретай.

5. **Добери то, чего продуктовый трек не даёт (пп. 4–6).** Это твоя реальная
   работа, а не переписывание:
   - **Нефункциональные требования** — нагрузка, отклик, доступность,
     приватность. Если `brief` называл ограничения (бюджет, регуляторка) —
     они уже здесь, не дублируй в НФТ.
   - **Точки интеграции** — внешние системы, API, источники данных. Смотри
     на единицы среза: что они предполагают, чего сами не называют явно.
   - **Ограничения** — бюджет, сроки, технологические, юридические — из
     `brief`, без пересочинения.
   Если добирать нечего — так и пиши, не заполняй ради заполнения.

6. **Решено и не пересматривается (п.7).** Продуктовые решения, которые
   delivery исполняет, а не оспаривает. Сюда же — причина, если какой-то
   delivery-гейт для этого разреза сознательно не будет пройден (см. «Правила
   движения» в `PROCESS.md`): решение владельца, не твоя догадка.

7. **Открытые вопросы (п.8).** Снимок текущих открытых вопросов на этот
   момент — из `.process/status.md`. Это **снимок**, не живая ссылка: дальше
   он не обновляется, актуальные вопросы по-прежнему в `status.md`.

8. **Как проверим вживую (п.9).** Для каждого критерия приёмки — чем именно
   он будет подтверждён: автотест, ручная проверка в реальном окружении,
   метрика после выпуска. Формулируется **сейчас**, не откладывается до
   код-ревью — иначе способ проверки подгонится под то, что получилось.
   Зелёные тесты на моках не доказательство для ветки, которая в проде ходит
   во внешнюю систему.

## Глубина сокращает состав

При глубине «только хребет» ([`PROCESS.md`](../../../process-core/PROCESS.md),
«Что означает глубина для всего трека») пишутся только пп. 1–3, 7 и 9 — без
4–6. Не заполняй их пустыми разделами «нет данных»: на этой глубине они не
предусмотрены, а не забыты. `handoff-review` на этой глубине их и не спросит.

При глубине «бриф и сразу код» `task_spec` не пишется вовсе — этот скилл не
вызывается.

## Структура `task_spec`

```markdown
# Task Spec: [название разреза]

> Разрез: `<task-id>` · Основано на: story_map, срез [Релиз N] · Глубина: [из brief]

## 1. Что делаем
Ссылка на единицы: [ID] — [одна строка], …

## 2. Критерии приёмки
- [US-XXX] [критерий]

## 3. Не делаем
- …

## 4. Нефункциональные требования
[или: «не предусмотрено на этой глубине»]

## 5. Точки интеграции
[или: «нет»]

## 6. Ограничения
[из brief]

## 7. Решено и не пересматривается
- …

## 8. Открытые вопросы (снимок на [дата])
- …

## 9. Как проверим вживую
| Критерий приёмки | Доказательство |
|---|---|
```

## Проверка перед выдачей

- [ ] Каждая единица среза имеет критерий приёмки.
- [ ] Каждый критерий приёмки — с доказательством из п. 9.
- [ ] Нет содержимого будущих релизов.
- [ ] На сокращённой глубине пп. 4–6 отсутствуют, а не пусты.
- [ ] Открытые вопросы класса Б (см. `PROCESS.md`) не остались нерешёнными.

## Результат

Запиши артефакт в `docs/delivery/<task-id>/TASK_SPEC.md` и верни:

```json
{"status": "done", "artifacts": ["docs/delivery/<task-id>/TASK_SPEC.md"], "assumptions": [], "blocking_questions": []}
```
</content>
